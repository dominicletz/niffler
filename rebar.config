{port_specs, [
    {"priv/niffler.nif.so", [
        "c_src/*.c"
    ]}
]}.

{port_env, [
    {"CFLAGS", "$CFLAGS -g -O3 -fPIC"},
    {"LDFLAGS", "-ldl -fPIC c_src/tinycc/libtcc.a"}
]}.

{plugins, [pc]}.

{pre_hooks, [
    {{pc, compile}, "./c_src/build_deps.sh"},
    {{pc, clean}, "make -C c_src/tinycc clean"}
]}.

{provider_hooks,
 [
  {pre,
   [
    {compile, {pc, compile}},
    {clean, {pc, clean}}
   ]
  }
 ]
}.